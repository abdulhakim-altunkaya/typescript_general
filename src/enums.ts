enum Direction {
    Up = 1,
    Down,
    Left,
    Right,
}
//it will print 4
console.log(Direction.Left);

//string enums
enum Orders {
    Ordered = "Ordered",
    Cooking = "Cooking",
    OnWay = "OnWay",
}

console.log(Orders.Cooking);